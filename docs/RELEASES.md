# Historical release inventory

The entries below record the pre-recovery release witnesses. As of the source-tree recovery, complete icon artwork and DarkCold image/font assets are also committed as ordinary files in their component directories. Releases are optional distribution/history material, never required inputs to routine validation, installation, or packaging.

---

# Release Inventory

## DarkCold Coonie 2.2.2

The `.tar.gz` and `.zip` witnesses are retained under `releases/darkcold/`. The editable tree under `themes/darkcold-coonie/` was extracted from that release with transient Python bytecode omitted.

## Coonie Aero Gel v1

The complete portable archive is under `releases/cursors/`. The editable tree includes the installable cursor files, alias set, build script, generated atlases, and the two original Library master sheets.

## Coonie's Aero Hoard 1.1.2

Git contains the small source/packaging archive, audit, preview, checksum ledger, and summary. The original release records report 115,933 installed icon files across 140 indexed directories.

The following Library-held binaries were not committed because each exceeds GitHub's 100 MB per-file limit:

| Artifact | Recorded size |
| --- | ---: |
| `Coonie-Aero-Hoard-1.1.2.tar.xz` | 202,854,188 bytes |
| `coonie-aero-hoard-icon-theme_1.1.2_all.deb` | 205,275,928 bytes |

Their expected hashes remain in `releases/icons/Coonie-Aero-Hoard-1.1.2-SHA256SUMS.txt`. Use those to verify a Library download, Git LFS migration, or GitHub Release upload.
