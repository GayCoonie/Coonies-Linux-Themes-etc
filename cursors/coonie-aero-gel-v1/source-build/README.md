# Coonie Aero Gel source bundle

Run `python3 build.py` to recreate the installable XCursor theme from the two
generated source atlases. See `THEME-README.md` for installation and design
notes. The finished theme is written to `dist/Coonie-Aero-Gel/`.
